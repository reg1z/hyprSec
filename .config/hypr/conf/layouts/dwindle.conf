# ----------------------------------------------------- 
# General window layout and colors
# name: "My Borders"
# ----------------------------------------------------- 

general {
  layout = dwindle
}

dwindle {
  pseudotile = true
  preserve_split = true
  force_split = 2
}

# Dwindle-Layout-Specific Binds
$mainMod = SUPER
$altMod = ALT

bind = $mainMod, S, layoutmsg, movetoroot active unstable
bind = $mainMod SHIFT, S, swapsplit # Swapsplit
